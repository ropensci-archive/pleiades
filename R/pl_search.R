#' Search for a place, name or X.
#'
#' This function searches a locally cached file of their data catalog.
#' You can update the cache if you want. See examples.
#'
#' @export
#' @param query A place ID
#' @param ... Further args passed on to \code{agrep}
#' @examples \dontrun{
#' pl_search(query='Rome')
#' }
pl_search <- function(query, ...){
  # load data
  # search for data
  # return data
  message("fxn not written yet")
}
